[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
asyncio_mode = auto
addopts = -ra --tb=short -q --strict-markers
markers =
    integration: marks integration tests
    slow: marks slow tests
filterwarnings =
    ignore::DeprecationWarning
